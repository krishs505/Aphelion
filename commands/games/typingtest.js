const { bot } = require("../../index");

module.exports = {
    name: 'typingtest',
    description: 'Test your typing speed!',
    aliases: ['tt'],
    usage: ' ',
    cooldown: 0,
    execute(message) {
        (async () => {
            
        })();
    }
}