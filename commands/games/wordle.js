const { bot } = require("../../index")

module.exports = {
    name: 'wordle',
    description: 'Solves a wordle.',
    usage: ' ',
    cooldown: 0,
    od: true,
    execute(message, args) {

        

    }
}